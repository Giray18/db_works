bundle:
  name: db_works

experimental:
  skip_name_prefix_for_schema: true

include:
  - resources/*.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: https://dbc-b4d14cbd-4705.cloud.databricks.com
